---
name: code-PR-verify-fallback

on:
  pull_request:

permissions:
  contents: read

jobs:
  unit-tests:
    name: Code / Verify
    runs-on: ubuntu-24.04
    steps:
      - run: 'echo "No Code / Verify required"'
